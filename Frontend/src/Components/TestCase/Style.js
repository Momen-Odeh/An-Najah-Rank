import {createUseStyles} from 'react-jss'

const useStyle = createUseStyles({
    customModal:{
        maxWidth: '800px',
        margin: '0 auto',
      },
      StrengthInput:{
        width:'80px'
      }
})

export default useStyle